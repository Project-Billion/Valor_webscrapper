services:
  scraper:
    image: ghcr.io/project-billion/valor_webscrapper:latest
    container_name: scraper
    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - ./output:/app/output
      - ./logs:/app/logs